# Phase 2 feature flags (default off)
COACH_CERT_DEEP_ENABLED=false
BRAIN_PROMPT_V2_SHADOW=false
TREATMENT_PLAN_ENABLED=false

# Coach check-in model (falls back to OPENAI_MODEL when empty)
# COACH_OPENAI_MODEL=gpt-6-sol
